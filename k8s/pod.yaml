apiVersion: v1
kind: Pod
metadata:
  name: "goserver"
  labels:
    app: "goserver"
spec:
  containers:
    - name: goserver
      image: "amravazzi/study-kubernetes:latest"
      # ports:
      # - name: liveness-port
      #   containerPort: 8000
